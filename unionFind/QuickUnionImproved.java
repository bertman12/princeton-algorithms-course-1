package unionFind;

public class QuickUnionImproved {
    /**
     * Weighted quick union by size
     * 
     * 
     * For union, point the smaller tree's root to the larger trees root.
     * This will flatten the trees 
     */
    
    //Elements point to their parent
    private int[] id;
    //Elements contain the size of their trees beneath them(inclusive); size is number of nodes that extend from a root
    private int[] size;

    



}
